# Loleza Ridge Lodge — final content setup

Before handing this site to the real lodge, replace the demo media and contact placeholders in `src/config.js`.

## Required client assets
- Real lodge photography for the room/gallery image paths.
- Verified phone number.
- Verified WhatsApp number in international format, digits only.
- Verified email.
- Verified Facebook, Instagram, TripAdvisor and Booking.com URLs.
- Verified Google Maps location URL/coordinates.
- Genuine guest testimonials only.

The code no longer uses Unsplash URLs. The bundled SVG media is intentionally local demo artwork so the deployment has no broken external image dependencies. Replace those local files with the client's licensed photography before the site is presented as the lodge's real photography.

## Main editable file
`src/config.js`
